# lofice local secrets (copy to .env.local — never commit)
# cp .env.local.example .env.local

# Neon Postgres (neonctl checkout main)
DATABASE_URL="postgresql://user:pass@host/neondb?sslmode=require"

# OpenAI — semantic embed (npm run embed)
OPENAI_API_KEY="sk-..."

# LOFICE_AI_ENDPOINT="https://api.openai.com/v1/chat/completions"
# LOFICE_AI_API_KEY="sk-..."
# LOFICE_AI_MODEL="gpt-4o-mini"
